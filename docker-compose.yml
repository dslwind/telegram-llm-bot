services:
  telegram-bot:
    build: .
    container_name: telegram-llm-bot
    network_mode: host
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data # stores SQLite data and runtime config.json
